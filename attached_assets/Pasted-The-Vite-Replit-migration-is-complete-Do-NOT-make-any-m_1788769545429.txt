The Vite/Replit migration is complete. Do NOT make any more frontend architecture changes or redesign the UI.

Now switch to FINAL SUBMISSION VERIFICATION of the complete PipelineGuard project.

We need to verify the actual backend + frontend integration, especially the two previously identified backend issues.

### 1. Backend Bug 1 — fresh database

Verify that a completely fresh database can handle:

POST /api/v1/demo/scenario/1

without manually running:

python -m app.seed

Previously this crashed because Repository.organization_id was NULL when no Organization existed.

Fix this properly and idempotently if it is still present.

Do NOT weaken the database constraint.

### 2. Backend Bug 2 — agentic demo scenarios

Verify the LIVE backend endpoints:

POST /api/v1/demo/scenario/1
POST /api/v1/demo/scenario/2
POST /api/v1/demo/scenario/3

The actual agentic decision engine must produce:

Scenario 1 → AUTO_FIX
Scenario 2 → PROPOSE_WITH_CAUTION
Scenario 3 → ESCALATE_TO_HUMAN

Do NOT hardcode the final API response.

Do NOT bypass the agentic decision engine.

Use the canonical assessment fields/types already used by the project so that each scenario naturally reaches the correct decision branch.

### 3. Preserve Replay Mode

Do NOT remove, rewrite, or replace:

frontend/src/lib/mock/scenarios.ts

Replay mode is intentionally fixture-backed and should continue working independently of the live backend.

### 4. Run backend tests

Run the complete backend test suite.

Report the exact result, e.g.:

XXX passed, 0 failed

Do not claim success without actually running it.

### 5. Run frontend verification

From frontend:

npm install
npm run build

The Vite build currently passes according to your previous work. Re-run it after the final changes.

### 6. End-to-end verification

Verify:

GET /health

Then verify all three live demo scenarios.

Then verify the frontend preview still loads:

* Overview
* Replay/demo screen
* all existing migrated routes

### 7. Git safety

Run:

git status
git diff --stat

Do NOT commit:

* .env
* API keys
* secrets
* node_modules
* .next
* dist/build artifacts if ignored
* virtual environments
* database files unless intentionally tracked

Do not delete unrelated project files.

### IMPORTANT

Do not stop at "the build passes."

I need the FINAL VERIFIED STATE of the entire PipelineGuard project.

At the end give me ONLY this summary:

1. Backend tests: exact result
2. Scenario 1: actual decision
3. Scenario 2: actual decision
4. Scenario 3: actual decision
5. Fresh database demo: PASS/FAIL
6. Backend health: PASS/FAIL
7. Frontend build: PASS/FAIL
8. Frontend routes: PASS/FAIL
9. Remaining blockers
10. Changed files

If anything fails, fix it and rerun the relevant verification before reporting.
