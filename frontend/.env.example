# Archive A FastAPI gateway. The default is the local backend.
# NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1

# Force replay mode on at boot (demo fallback). Also togglable in the UI header.
# NEXT_PUBLIC_REPLAY=1
